#include "load.h"

void load(PtList list) {
	printf("Em que ano pretende fazer a previsão? ");

	printf("Insira os nomes dos dois ficheiros: ");

	printf("Foram lidos %d registos de unidades curriculares e %d rgistos de inscrições", , );
}